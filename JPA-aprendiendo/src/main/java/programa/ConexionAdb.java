package programa;

public class ConexionAdb {
	
	
	/*
	 si estamos usando jee podemos hacer	
	
	@PersistenceContext(unitName = "ujyt" )
	private static EntityManager manager;
	*/
	
	/*
	 la otra forma es usar un entity manager factory
	
	@PersistenceUnit(unitName = "ujyt" )
	private static EntityManagerFactory manager;
	*/
	
}
